# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Vendored @smogon/calc build (see vendor/smogon-calc/README.md)
!vendor/smogon-calc/dist/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OpenSpec artifacts
.openspec/changes/*
!.openspec/changes/.keep

# Python
.venv/*

# High-resolution artwork is fetched/generated on demand.
official-artwork/
public/images/pokemon/official-artwork/
# scan crop debug output
training/.crop-preview/
training/sprite-net-checkpoint.pt

# browser-decoded HP fixtures (regenerate: python3 scripts/capture-browser-fixtures.py)
training/hp-fixtures/
training/.hp-debug/
training/.hp-character-preview.png
hp-reader/dataset-candidates/

# Vendored ONNX Runtime WASM (reproducible: npm run vendor:ort)
public/ort/
# Derived sips PNG conversions of jpg/heic screenshots (regenerated on demand)
training/.converted-screenshots/
# Tesseract data — OCR was evaluated and rejected (see scripts/ocr-spike.ts)
eng.traineddata
# HP screenshot staging inboxes for the add/review labeling flow
hp-reader/new-screenshot/
hp-reader/new-candidate/

# Experimental HP glyph-net artifacts (reproducible via scripts/train_glyph_net.py)
hp-reader/models/glyph-net-checkpoint.pt
hp-reader/models/hp_classifier.onnx

# Python bytecode
__pycache__/
*.pyc
# Colab dataset bundle (regenerate: zip -r glyph-dataset.zip hp-reader/dataset scripts/train_glyph_net.py)
glyph-dataset.zip

# Local scratch (planning-with-files + tools)
findings.md
progress.md
task_plan.md
.antigravitycli/
.worktrees/
training/screenshots/

# player team scan golden screenshots (local only)
training/player-screens/
# player panel crop debug output
training/.player-crop-preview/

# browser-decoded player stats fixtures (regenerate: python3 scripts/capture-player-browser-fixtures.py)
training/player-fixtures/
training/panel-crops/

# Local ML checkpoints
training/*.pt
